import { AvatarPlaceholderDirective } from './avatar-placeholder.directive';

describe('AvatarPlaceholderDirective', () => {
  it('should create an instance', () => {
    const directive = new AvatarPlaceholderDirective();
    expect(directive).toBeTruthy();
  });
});
