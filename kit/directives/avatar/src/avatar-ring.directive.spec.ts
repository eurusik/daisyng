import { AvatarRingDirective } from './avatar-ring.directive';

describe('AvatarRingDirective', () => {
  it('should create an instance', () => {
    const directive = new AvatarRingDirective();
    expect(directive).toBeTruthy();
  });
});
