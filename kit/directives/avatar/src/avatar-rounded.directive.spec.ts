import { AvatarRoundedDirective } from './avatar-rounded.directive';

describe('AvatarRoundedDirective', () => {
  it('should create an instance', () => {
    const directive = new AvatarRoundedDirective();
    expect(directive).toBeTruthy();
  });
});
