import { AlertDescriptionDirective } from './alert-description.directive';

describe('AlertDescriptionDirective', () => {
  it('should create an instance', () => {
    const directive = new AlertDescriptionDirective();
    expect(directive).toBeTruthy();
  });
});
