import { ButtonVariants } from './button.config';

/**
 * Type representing the width variants of a button.
 *
 * - `default`: No specific width applied.
 * - `full`: The button takes up the full width of its container.
 * - `wide`: The button is wider than the default size.
 *
 */
export type ButtonWidth = ButtonVariants['width'];
