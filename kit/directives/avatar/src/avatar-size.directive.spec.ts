import { AvatarSizeDirective } from './avatar-size.directive';

describe('AvatarSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new AvatarSizeDirective();
    expect(directive).toBeTruthy();
  });
});
